<odoo>
    <data noupdate="1">
        <record id="ir_cron_fetch_and_update_tracking_details" model="ir.cron">
            <field name="name">Fetch and Update Tracking Details</field>
            <!-- Adjust the model_id reference to match your model's technical name -->
            <field name="model_id" ref="model_deepu_sale_tracking"/>
            <field name="state">code</field>
            <!-- Ensure your method call matches the method defined in your model -->
            <field name="code">model.fetch_and_update_tracking_details()</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">4</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field> <!-- Runs indefinitely -->
        </record>

        <record id="ir_cron_shipsgo_tracking_check" model="ir.cron">
            <field name="name">ShipsGo: Check and set is_shipsgo_tracking</field>
            <field name="model_id" ref="model_shipsgo_tracking_update"/>
            <field name="state">code</field>
            <field name="code">model.run_shipsgo_check()</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">4</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
        </record>
    </data>
</odoo>
